(() => {
  "use strict";

  // Generated Lite builds replace this file. The normal extension ships with
  // every bundle enabled. Keeping the profile separate means the runtime and
  // the future website builder can agree on the same bundle boundaries.
  window.__DSQ_BUILD_PROFILE__ = Object.freeze({
    id: "full",
    label: "Full",
    generated: false,
    bundles: Object.freeze([
      "core",
      "interface",
      "chat",
      "chatList",
      "listings",
      "creator",
      "lorebook",
      "profiles",
      "personas"
    ])
  });
})();
